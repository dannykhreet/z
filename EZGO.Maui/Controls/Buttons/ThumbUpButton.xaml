<?xml version="1.0" encoding="utf-8" ?>
<button:SfButton
    x:Class="EZGO.Maui.Controls.Buttons.ThumbUpButton"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:button="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
    xmlns:classes="clr-namespace:EZGO.Maui.Classes"
    xmlns:enumerations="clr-namespace:EZGO.Api.Models.Enumerations;assembly=EZGO.Api.Models"
    x:Name="okButton"
    Background="Transparent"
    CommandParameter="{Binding}"
    Stroke="{Binding ButtonColor, Source={x:Reference okButton}}"
    StrokeThickness="4">
    <button:SfButton.Triggers>
        <DataTrigger
            Binding="{Binding Status, Source={x:Reference okButton}}"
            TargetType="button:SfButton"
            Value="{x:Static enumerations:TaskStatusEnum.Ok}">
            <Setter Property="Background" Value="{StaticResource GreenColor}" />
            <Setter Property="Stroke" Value="{StaticResource GreenColor}" />
        </DataTrigger>
    </button:SfButton.Triggers>
    <button:SfButton.Content>
        <DataTemplate>
            <Image
                IsOpaque="True"
                HorizontalOptions="CenterAndExpand"
                VerticalOptions="CenterAndExpand">
                <Image.Source>
                    <FontImageSource
                        x:Name="imageSource"
                        FontFamily="{StaticResource MaterialFontFamily}"
                        Glyph="{x:Static classes:IconFont.ThumbUp}"
                        Size="25"
                        Color="{Binding IconColor, Source={x:Reference okButton}}" />
                </Image.Source>
            </Image>
        </DataTemplate>
    </button:SfButton.Content>
    <VisualStateManager.VisualStateGroups>
        <VisualStateGroup x:Name="CommonStates">
            <VisualState x:Name="Disabled">
                <VisualState.Setters />
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
</button:SfButton>
