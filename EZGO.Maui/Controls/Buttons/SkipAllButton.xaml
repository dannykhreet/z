<button:SfButton
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:button="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
    x:Class="EZGO.Maui.Controls.Buttons.SkipAllButton"
    xmlns:classes="clr-namespace:EZGO.Maui.Classes"
    WidthRequest="100"
    CornerRadius="0"
    HeightRequest="30"
    StrokeThickness="3"
    Stroke="{StaticResource SkippedColor}"
    x:Name="skipButton">
    <button:SfButton.Content>
        <DataTemplate>
            <StackLayout Orientation="Horizontal" Spacing="5" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                <Label
                    Text="{classes:Translate SKIP_ALL_TASKS}"
                    VerticalOptions="CenterAndExpand"
                    TextColor="{Binding TextColorButton, Source={x:Reference skipButton}}"
                    FontSize="13"/>
                <Image>
                    <Image.Source>
                        <FontImageSource
                            FontFamily="{StaticResource MaterialFontFamily}"
                            Glyph="{x:Static classes:IconFont.FastForward}"
                            Size="20"
                            Color="{Binding IconColor, Source={x:Reference skipButton}}"/>
                    </Image.Source>
                </Image>
            </StackLayout>
        </DataTemplate>
    </button:SfButton.Content>
</button:SfButton>
