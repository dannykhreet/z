<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="EZGO.Maui.Views.Assessments.AssessmentsInstructionItemsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:auditModels="clr-namespace:EZGO.Maui.Core.Models.Audits;assembly=EZGO.Maui.Core"
    xmlns:buttons="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
    xmlns:controls="clr-namespace:EZGO.Maui.Controls"
    xmlns:customButtons="clr-namespace:EZGO.Maui.Controls.Buttons"
    xmlns:datatemplates="clr-namespace:EZGO.Maui.Controls.DataTemplates"
    xmlns:labels="clr-namespace:EZGO.Maui.Controls.CustomLabels"
    xmlns:classes="clr-namespace:EZGO.Maui.Classes"
    xmlns:models="clr-namespace:EZGO.Maui.Core.Models.Assessments;assembly=EZGO.Maui.Core"
    xmlns:viewModels="clr-namespace:EZGO.Maui.Core.ViewModels.Assessments;assembly=EZGO.Maui.Core"
    xmlns:popup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
    xmlns:xForms="clr-namespace:Syncfusion.Maui.ListView;assembly=Syncfusion.Maui.ListView"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
		xmlns:images="clr-namespace:EZGO.Maui.Controls.Images"
	ios:Page.UseSafeArea="true"
    x:DataType="viewModels:AssessmentInstructionItemsViewModel"
    NavigationPage.HasBackButton="True"
    NavigationPage.HasNavigationBar="False"
    x:Name="Page">
    <ContentPage.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="GridItemTemplate" x:DataType="models:BasicAssessmentInstructionItemModel">
                <Grid RowSpacing="0">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding BindingContext.NavigateToCarouselViewCommand, Source={x:Reference Page}}" CommandParameter="{Binding}" />
                    </Grid.GestureRecognizers>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="5" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="100" />
                    </Grid.RowDefinitions>

                    <!--  Image  -->
                    <controls:CustomImageOnline
                        Grid.Row="1"
                        Grid.RowSpan="2"
                        Aspect="Fill"
                        ImageUrl="{Binding DisplayPicture}"
                        IsOpaque="True" />

                    <!--  Overlay  -->
                    <BoxView Grid.Row="2" BackgroundColor="LightGray" Opacity="0.6"/>

                    <!--  Name  -->
                    <StackLayout Grid.Row="2" VerticalOptions="CenterAndExpand" Margin="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height ="30"/>
                                <RowDefinition Height ="30"/>
                                <RowDefinition Height ="30"/>
                            </Grid.RowDefinitions>

                            <Label
                                Grid.Column="0"
                                Grid.Row="0"
                                VerticalOptions="Center"
                                FontFamily="FAR"
                                FontSize="Large"
                                Text="input-text"/>
                            <Label
                                Grid.Column="1"
                                Grid.Row="0"
                                FontSize="Body"
                                MaxLines="2"
                                Text="{Binding Name}"
                                VerticalOptions="Center"
                                TextColor="Black"/>
                            <Label
                                Grid.Column="0"
                                Grid.Row="1"
                                VerticalOptions="Center"
                                FontFamily="FAR"
                                FontSize="Medium"
                                IsVisible="{Binding IsCompleted}"
                                Text="user-group"/>
                            <Label 
                                Grid.Column="1"
                                Grid.Row="1"
                                Text="{Binding ModifiedByName}"
                                IsVisible="{Binding IsCompleted}"
                                FontSize="Medium"/>
                            <Label
                                Grid.Column="0"
                                Grid.Row="2"
                                VerticalOptions="Center"
                                FontFamily="FAR"
                                FontSize="Medium"
                                IsVisible="{Binding IsCompleted}"
                                Text="calendar"/>
                            <Label
                                Grid.Column="1"
                                Grid.Row="2"
                                IsVisible="{Binding IsCompleted}"
                                FontSize="Medium"
                                VerticalOptions="Center"
                                Text ="{Binding LocalCompletedAt, Converter={StaticResource DateTimeShortMonthToStringConverter}}"/>
                            <Label
                                Grid.Column="1"
                                Grid.Row="2"
                                TextColor="GreenYellow"
                                IsVisible="{Binding IsCompleted ,Converter={StaticResource ReverseBooleanConverter}}"
                                FontSize="Medium"
                                VerticalOptions="Center"
                                Text="Score Instructions"/>
                        </Grid>

                    </StackLayout>

                    <!--  Buttons  -->
                    <!--  Score Button  -->
                    <customButtons:ScoreButton
                        x:Name="ScoreButton"
                        Grid.Row="1"
                        Margin="10"
                        ColorCalculator="{Binding BindingContext.ColorCalculator, Source={x:Reference Page}}"
                        Command="{Binding BindingContext.OpenScoreCommand, Source={x:Reference Page}}"
                        CommandParameter="{Binding .}"
                        EmptyBackgroundColor="{StaticResource WhiteColor}"
                        HorizontalOptions="End"
                        Score="{Binding Score}"
                        Style="{StaticResource WhiteBorderButton}"
                        VerticalOptions="Start" />
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="LinearItemTemplate" x:DataType="models:BasicAssessmentInstructionItemModel">

                <Grid BackgroundColor="White">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding BindingContext.NavigateToCarouselViewCommand, Source={x:Reference Page}}" CommandParameter="{Binding}" />
                    </Grid.GestureRecognizers>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>
                    <!--  Item Start  -->

                    <!--  Item Image  -->
                    <controls:CustomImageOnline
                        Grid.Column="0"
                        Aspect="AspectFill"
                        HeightRequest="105"
                        ImageUrl="{Binding DisplayPicture}"
                        IsOpaque="True" />

                    <!--  Item Name  -->
                    <VerticalStackLayout 
                        Grid.Column="1" 
                        Grid.Row="0" 
                        VerticalOptions="Center"
                        Margin="20,0,0,0" >
                        <Label
                            Text="{Binding Name}"
                            TextColor="Black"
                            FontSize="Medium"/>
                        <HorizontalStackLayout Margin="10,20,0,0">
                            <images:RoundUserImage
                                Grid.Column="1"
                                HorizontalOptions="Start"
                                UserId="{Binding ModifiedById}"/>
                            <Label 
                                Margin="10,0,0,0"
                                FontSize="Medium"
                                IsVisible="{Binding IsCompleted}"
                                Text ="Assessors: "/>
                            <Label 
                                Text="{Binding ModifiedByName}"
                                FontSize="Medium"/>
                            <Label
                                IsVisible="{Binding IsCompleted}"
                                FontSize="Medium"
                                Text ="{Binding LocalCompletedAt, StringFormat=', {0}', Converter={StaticResource DateTimeShortMonthToStringConverter}}"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                    <!--  Item Buttons  -->
                    <!--  Score Button  -->
                    <customButtons:ScoreButton
                        x:Name="ScoreButton"
                        Grid.Row="0"
                        Grid.Column="2"
                        Margin="0,0,15,0"
                        ColorCalculator="{Binding BindingContext.ColorCalculator, Source={x:Reference Page}}"
                        Command="{Binding BindingContext.OpenScoreCommand, Source={x:Reference Page}}"
                        CommandParameter="{Binding .}"
                        EmptyBackgroundColor="{StaticResource GreyColor}"
                        Score="{Binding Score}"
                        Style="{StaticResource GreyBorderButton}"
                        VerticalOptions="Center" />
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>


    <ContentPage.Content>
        <AbsoluteLayout
            AbsoluteLayout.LayoutBounds="0,0,1,1"
            AbsoluteLayout.LayoutFlags="WidthProportional,HeightProportional"
            CompressedLayout.IsHeadless="True">
            <!--  Assessment score popup  -->
            <popup:SfPopup
                x:Name="ScorePopup"
                Margin="0"
                Background="White"
                HeightRequest="75"
                ShowFooter="False"
                ShowHeader="False"
                ShowOverlayAlways="False"
                WidthRequest="{Binding ScoreWidth}">
                <popup:SfPopup.PopupStyle>
                    <popup:PopupStyle CornerRadius="20" />
                </popup:SfPopup.PopupStyle>
                <popup:SfPopup.ContentTemplate>
                    <DataTemplate>
                        <StackLayout
                            Padding="0">
                            <xForms:SfListView
                                Margin="0"
                                Padding="5"
                                CachingStrategy="RecycleTemplate"
                                ItemSpacing="3"
                                ItemsSource="{Binding Scores}"
                                Orientation="Horizontal">
                                <xForms:SfListView.ItemTemplate>
                                    <DataTemplate x:DataType="auditModels:ScoreModel">
                                        <ViewCell>
                                            <buttons:SfButton
                                                Margin="0,10,0,0"
                                                Background="{Binding Color}"
                                                Command="{Binding BindingContext.TaskScoreCommand, Source={x:Reference Page}}"
                                                CommandParameter="{Binding Number}"
                                                Style="{StaticResource ScoreButton}"
                                                VerticalOptions="Start">
                                                <buttons:SfButton.Content>
                                                    <DataTemplate>
                                                        <Label
                                                            FontSize="Medium"
                                                            HorizontalOptions="CenterAndExpand"
                                                            Text="{Binding Number}"
                                                            TextColor="White"
                                                            VerticalOptions="CenterAndExpand" />
                                                    </DataTemplate>
                                                </buttons:SfButton.Content>
                                            </buttons:SfButton>
                                        </ViewCell>
                                    </DataTemplate>
                                </xForms:SfListView.ItemTemplate>
                            </xForms:SfListView>
                        </StackLayout>
                    </DataTemplate>
                </popup:SfPopup.ContentTemplate>
            </popup:SfPopup>


            <Grid
                AbsoluteLayout.LayoutBounds="0,0,1,1"
                AbsoluteLayout.LayoutFlags="WidthProportional,HeightProportional"
                BackgroundColor="{StaticResource BackgroundColor}"
                ColumnSpacing="0"
                RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.16*" />
                    <RowDefinition Height="0.09*" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <!--  Header  -->
                <StackLayout Grid.Row="0" BackgroundColor="White">
                    <Grid
                        Padding="0,5,0,0"
                        ColumnSpacing="0"
                        RowSpacing="0"
                        StyleClass="header, bg-white, m-0, pr-15"
                        VerticalOptions="FillAndExpand">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="10*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!--  Back Button  -->
                        <customButtons:BackButton
                            Grid.Column="0"
                            Command="{Binding BindingContext.CancelCommand, Source={x:Reference Page}}"
                            HorizontalOptions="Start" />

                        <!--  Title Label  -->
                        <labels:TitleLabel
                            Grid.Column="1"
                            HorizontalTextAlignment="Start"
                            Text="{Binding CurrentAssessmentSkillInstruction.Name}" />

                        <!--  Layout Buttons  -->
                        <StackLayout
                            Grid.Column="2"
                            HorizontalOptions="EndAndExpand"
                            Orientation="Horizontal"
                            StyleClass="column2"
                            VerticalOptions="Center">

                            <!--  Layout Switcher  -->
                            <controls:LayoutSwitcher
                                HorizontalOptions="Center"
                                ListLayout="{Binding ListViewLayout}"
                                TapCommand="{Binding ListViewLayoutCommand}"
                                VerticalOptions="Center" />
                        </StackLayout>
                    </Grid>

                    <!--  Tags  -->
                    <xForms:SfListView
                        Margin="5,0,0,5"
                        HeightRequest="55"
                        IsVisible="{Binding CurrentAssessmentSkillInstruction.Tags, Converter={StaticResource ListCountToBooleanConverter}}"
                        ItemSize="166"
                        ItemsSource="{Binding CurrentAssessmentSkillInstruction.Tags}"
                        Orientation="Horizontal"
                        ScrollBarVisibility="Never"
                        SelectionMode="None">
                        <xForms:SfListView.ItemTemplate>
                            <datatemplates:TagDataTemplate />
                        </xForms:SfListView.ItemTemplate>
                    </xForms:SfListView>
                </StackLayout>

                <!--  Buttons  -->
                <Grid
                    Grid.Row="1"
                    Margin="10,10,10,0"
                    BackgroundColor="White">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="33*" />
                        <ColumnDefinition Width="33*" />
                        <ColumnDefinition Width="33*" />
                    </Grid.ColumnDefinitions>
                    <!--  Left button  -->
                    <buttons:SfButton
                        x:Name="leftButton"
                        Grid.Column="0"
                        Background="Transparent"
                        Command="{Binding BindingContext.PreviousCommand, Source={x:Reference Page}}"
                        HorizontalOptions="StartAndExpand"
                        IsVisible="{Binding IsLeftArrowVisible}"
                        VerticalOptions="CenterAndExpand">
                        <buttons:SfButton.Content>
                            <DataTemplate>
                                <Image
                                    HorizontalOptions="StartAndExpand"
                                    IsOpaque="True"
                                    VerticalOptions="CenterAndExpand">
                                    <Image.Source>
                                        <FontImageSource
                                            FontFamily="{StaticResource MaterialFontFamily}"
                                            Glyph="{x:Static classes:IconFont.ArrowBack}"
                                            Size="30"
                                            Color="{StaticResource GreenColor}" />
                                    </Image.Source>
                                </Image>
                            </DataTemplate>
                        </buttons:SfButton.Content>
                    </buttons:SfButton>

                    <!--  Home button  -->
                    <buttons:SfButton
                        Grid.Column="1"
                        Background="Transparent"
                        Command="{Binding BindingContext.CancelCommand, Source={x:Reference Page}}"
                        HorizontalOptions="Center"
                        VerticalOptions="Center">
                        <buttons:SfButton.Content>
                            <DataTemplate>
                                <Grid
                                    Padding="10"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="FillAndExpand"
                                    VerticalOptions="FillAndExpand">
                                    <Image
                                        HorizontalOptions="FillAndExpand"
                                        IsOpaque="True"
                                        VerticalOptions="FillAndExpand">
                                        <Image.Source>
                                            <FontImageSource
                                                FontFamily="MyAlias"
                                                Glyph="{StaticResource HomeIcon}"
                                                Size="20"
                                                Color="{x:StaticResource GreenColor}" />
                                        </Image.Source>
                                    </Image>
                                </Grid>
                            </DataTemplate>
                        </buttons:SfButton.Content>
                    </buttons:SfButton>

                    <!--  Right button  -->
                    <buttons:SfButton
                        x:Name="rightButton"
                        Grid.Column="2"
                        Background="Transparent"
                        Command="{Binding BindingContext.NextCommand, Source={x:Reference Page}}"
                        HorizontalOptions="EndAndExpand"
                        IsVisible="{Binding IsRightArrowVisible}"
                        VerticalOptions="CenterAndExpand">
                        <buttons:SfButton.Content>
                            <DataTemplate>
                                <Image
                                    HorizontalOptions="EndAndExpand"
                                    IsOpaque="True"
                                    VerticalOptions="CenterAndExpand">
                                    <Image.Source>
                                        <FontImageSource
                                            FontFamily="{StaticResource MaterialFontFamily}"
                                            Glyph="{x:Static classes:IconFont.ArrowForward}"
                                            Size="30"
                                            Color="{StaticResource GreenColor}" />
                                    </Image.Source>
                                </Image>
                            </DataTemplate>
                        </buttons:SfButton.Content>
                    </buttons:SfButton>
                </Grid>

                <Grid
                    x:Name="BottomRowGrid"
                    Grid.Row="2"
                    ColumnSpacing="0"
                    CompressedLayout.IsHeadless="True">

                    <!--  Detail page  -->
                    <Grid CompressedLayout.IsHeadless="True">
                        <xForms:SfListView
                            x:Name="gridListView"
                            BackgroundColor="{StaticResource BackgroundColor}"
                            CachingStrategy="CreateNewTemplate"
                            HeaderSize="50"
                            IsVisible="{Binding IsListVisible, Converter={StaticResource ReverseBooleanConverter}}"
                            ItemSize="280"
                            ItemSpacing="5"
                            ItemTemplate="{StaticResource GridItemTemplate}"
                            ItemsSource="{Binding InstructionsFilter.FilteredList}"
                            SelectionMode="None">
                            <xForms:SfListView.ItemsLayout>
                                <xForms:GridLayout SpanCount="3" />
                            </xForms:SfListView.ItemsLayout>

                        </xForms:SfListView>

                        <xForms:SfListView
                            x:Name="linearListView"
                            BackgroundColor="{StaticResource BackgroundColor}"
                            HeaderSize="50"
                            IsVisible="{Binding IsListVisible}"
                            ItemSize="110"
                            ItemSpacing="7"
                            ItemTemplate="{StaticResource LinearItemTemplate}"
                            ItemsSource="{Binding InstructionsFilter.FilteredList}"
                            SelectionMode="None" />

                        <controls:EmptyView IsVisible="{Binding HasItems, Converter={StaticResource ReverseBooleanConverter}}" />

                    </Grid>


                    <!--  Message Center  -->
                    <controls:MessageCenter />
                </Grid>
            </Grid>

            <!--  Popup Overlay  -->
            <Grid
                AbsoluteLayout.LayoutBounds="0,0,1,1"
                AbsoluteLayout.LayoutFlags="WidthProportional,HeightProportional"
                IsVisible="{Binding IsDropdownOpen}">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding BindingContext.CloseDropdownCommand, Source={x:Reference Page}}" />
                </Grid.GestureRecognizers>
            </Grid>

        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>
